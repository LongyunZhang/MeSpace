<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pku.mespace" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 摄像头权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- SD卡读写权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
     android:theme="@android:style/Theme.NoTitleBar"
          android:theme="@style/AppTheme"
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" >
        </activity>
        <activity
            android:name=".ItemListActivity"
            android:label="@string/title_activity_info_lists" >
        </activity>
        <activity
            android:name=".ItemDetailedActivity"
            android:label="@string/title_activity_item_detailed" >
        </activity>
        <activity
            android:name=".ItemAddActivity"
            android:label="@string/title_activity_item_add" >
        </activity>
        <activity
            android:name=".FileAddActivity"
            android:label="@string/title_activity_file_add" >
        </activity>
        <activity
            android:name=".FileEditActivity"
            android:label="@string/title_activity_file_edit" >
        </activity>
    </application>

</manifest>
